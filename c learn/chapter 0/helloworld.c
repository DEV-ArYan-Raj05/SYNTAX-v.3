#include <stdio.h>// header file for input output functions , also k/a PRE PROCESSOR DIRECTIVE
int main(){// main function - starting point of the program
    printf("hello world");// tells which output to show on the screen
    return 0;//returning 0 to OS
}