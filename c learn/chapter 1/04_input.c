#include<stdio.h>
int main(){
    int a;//declaring variable,but not initializing it
    printf("enter the number");//asking user to input
    scanf("%d",&a);//scanning or taking the input in memory
   printf("%d is number in variable",a);//printing variable as output as per user
   return 0;
}