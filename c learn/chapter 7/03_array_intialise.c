#include <stdio.h>

int main() {
int cgpa[3] ={1,2,3};/*for array of lesser value i can just use them in 
{}to initialise the values */
for(int i=0; i<3;i++){
    printf("value of cgpa %d is %d\n",i , cgpa[i]);
}
    return 0;
}